services:
  ai-navigator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-navigator-app
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production